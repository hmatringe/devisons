<%= simple_form_for(@bike) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :speed %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
